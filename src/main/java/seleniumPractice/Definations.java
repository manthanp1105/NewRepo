package seleniumPractice;

public class Definations {
	
	// Difference Between Set and List?
	// Set cannot take duplicate values. Only unique. Does not maintain indexing. It is index less collections. So no 0,1,2,3
	// so you cannot have get(0) or get(1) elements bcz no index.
	// iterator()method is used for indexless collection. 
}
